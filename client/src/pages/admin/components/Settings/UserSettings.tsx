import { Container } from "@mui/material";
import React from "react";

const UserSettings = () => {
  return (
    <Container maxWidth="xl">
      <div className="bg-gray-100">UserSettings</div>
    </Container>
  );
};

export default UserSettings;
